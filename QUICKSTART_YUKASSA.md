# Быстрый старт: Интеграция ЮKassa

## Что было сделано

Интеграция платежной системы ЮKassa для приема оплаты подписок в Telegram боте:

✅ Добавлен SDK ЮKassa (yookassa==3.0.0)
✅ Создан модуль yukassa_payment.py для работы с платежами
✅ Добавлены таблицы БД: payments и payment_methods
✅ Реализовано UI для выбора тарифов и управления картами
✅ Настроено сохранение платежных методов для автоплатежей
✅ Реализована проверка оплаты по кнопке (без webhook)
✅ Добавлена документация по настройке (YUKASSA_SETUP.md)

## Шаги для запуска

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте .env файл

Отредактируйте `.env` и добавьте параметры ЮKassa:

```env
# ЮKassa конфигурация
YUKASSA_SHOP_ID=ваш_shop_id
YUKASSA_SECRET_KEY=ваш_secret_key
YUKASSA_TEST_MODE=True
```

**Где взять ключи:**
- Зайдите в личный кабинет ЮKassa: https://yookassa.ru/
- Перейдите в "Настройки" → "API"
- Скопируйте Shop ID и Secret Key

### 3. Запустите бота

```bash
python main.py
```

Вы должны увидеть:
```
Бот запущен
```

### 4. Протестируйте оплату

1. Откройте бота в Telegram
2. Нажмите "💳 Подписка"
3. Выберите тариф
4. Нажмите "💳 Оплатить" - откроется страница ЮKassa
5. Используйте тестовую карту: **5555 5555 5555 4477**
6. Вернитесь в бот и нажмите "✅ Проверить оплату"
7. Подписка активируется автоматически!

## Основные функции

### Для пользователей

- **Оформление подписки**: Выбор тарифа и оплата через ЮKassa
- **Управление картами**: Просмотр и отвязка привязанных карт
- **Проверка оплаты по кнопке**: Пользователь сам проверяет статус после оплаты
- **Автоматическая активация**: Подписка активируется при проверке
- **Сохранение карт**: Карта автоматически сохраняется при первой оплате

### Для администратора

- **Проверка через API**: Статус платежа проверяется через API ЮKassa
- **База данных**: Хранение платежей, подписок и привязанных карт
- **Логирование**: Все операции логируются для отладки
- **Без внешних зависимостей**: Не требуется домен или ngrok

## Структура файлов

```
wbproducts_final/
├── main.py                  # Основной файл бота (обновлен)
├── yukassa_payment.py       # Модуль работы с ЮKassa API (новый)
├── database.py              # БД с новыми методами (обновлен)
├── keyboards.py             # Клавиатуры с управлением картами (обновлен)
├── config.py                # Конфигурация (обновлен)
├── .env.example             # Пример конфигурации (обновлен)
├── requirements.txt         # Зависимости (обновлен)
├── YUKASSA_SETUP.md         # Подробная документация (новый)
└── QUICKSTART_YUKASSA.md    # Этот файл (новый)
```

## Что дальше?

### Для получения скриншотов:

1. Запустите бота с тестовыми ключами
2. Пройдите процесс оплаты с тестовой картой
3. Сделайте скриншоты:
   - Выбор тарифа
   - Страница оплаты ЮKassa
   - Подтверждение оплаты
   - Активированная подписка
   - Управление картами

### Для боевого режима:

1. Получите боевые ключи в ЛК ЮKassa
2. Настройте постоянный URL для webhook (без ngrok!)
3. Обновите `.env`: `YUKASSA_TEST_MODE=False`
4. Перезапустите бота

## Автоплатежи (будущая функция)

Для автоматического продления подписки:

```python
# В yukassa_payment.py уже есть поддержка:
payment_data["payment_method_id"] = saved_card_id

# Это позволит списывать деньги без перенаправления пользователя
```

Реализация:
1. Создать фоновую задачу проверки истекающих подписок
2. Для подписок с привязанными картами создавать платеж с payment_method_id
3. Уведомлять пользователя об успешном/неуспешном продлении

## Troubleshooting

### "ЮKassa не настроена"

- Проверьте `.env`: должны быть YUKASSA_SHOP_ID и YUKASSA_SECRET_KEY
- Перезапустите бота

### Платеж создается но не активируется

- Убедитесь что оплата прошла успешно в ЮKassa
- Нажмите кнопку "Проверить оплату" в боте
- Если платеж в статусе "pending" - подождите минуту и проверьте еще раз
- Смотрите логи бота для деталей

### Кнопка "Проверить оплату" не работает

- Проверьте интернет-соединение сервера с ботом
- Убедитесь что ключи ЮKassa правильные
- Проверьте логи на наличие ошибок API

## Полезные команды

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python main.py

# Просмотр логов (если настроен файл логов)
tail -f bot.log
```

## Контакты

- Документация ЮKassa: https://yookassa.ru/docs
- Техподдержка ЮKassa: support@yookassa.ru
- Тестовые данные: https://yookassa.ru/docs/support/payments/testing

---

**Готово к тестированию!** 🎉
