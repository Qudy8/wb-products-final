# Настройка ЮKassa для приема платежей

Этот документ описывает процесс настройки платежей через ЮKassa в Telegram боте.

## Шаг 1: Регистрация в ЮKassa

1. Перейдите на сайт [ЮKassa](https://yookassa.ru/)
2. Зарегистрируйтесь как продавец
3. Заполните все необходимые данные о компании/ИП

## Шаг 2: Получение тестовых ключей

Для тестирования платежей ЮKassa предоставляет тестовую среду:

1. Войдите в личный кабинет ЮKassa
2. Перейдите в раздел **Настройки** → **Настройки магазина**
3. Переключитесь в **Тестовый режим** (если доступен)
4. Найдите секцию **API**
5. Скопируйте:
   - **shopId** (идентификатор магазина)
   - **Секретный ключ** (secret key)

### Тестовые карты

Для тестирования можно использовать тестовые карты ЮKassa:

- **Успешная оплата**: `5555 5555 5555 4477`
  - CVC: любой (3 цифры)
  - Срок: любая будущая дата
  - 3D-Secure код: любой (если запросят)

- **Отклоненная оплата**: `5555 5555 5555 4444`

## Шаг 3: Настройка webhook

Webhook необходим для получения уведомлений о статусе платежей.

### 3.1. Получение публичного URL

Для webhook нужен публичный URL. Варианты:

#### Вариант A: Использование ngrok (для тестов)

1. Скачайте [ngrok](https://ngrok.com/)
2. Запустите ngrok:
   ```bash
   ngrok http 8080
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

#### Вариант B: Собственный сервер

Если у вас есть сервер с доменом и SSL-сертификатом, используйте его URL.

### 3.2. Настройка в ЮKassa

1. В личном кабинете ЮKassa перейдите в **Настройки** → **Уведомления**
2. Включите **HTTP-уведомления**
3. Укажите URL для уведомлений: `https://your-domain.com/yukassa/webhook`
4. Сохраните настройки

## Шаг 4: Конфигурация бота

### 4.1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Убедитесь, что в `requirements.txt` есть:
```
yookassa==3.0.0
```

### 4.2. Настройка .env файла

Скопируйте `.env.example` в `.env`:
```bash
copy .env.example .env
```

Заполните параметры ЮKassa в `.env`:

```env
# ЮKassa конфигурация
YUKASSA_SHOP_ID=your_shop_id_here
YUKASSA_SECRET_KEY=your_secret_key_here
YUKASSA_TEST_MODE=True
YUKASSA_WEBHOOK_URL=https://your-domain.com/yukassa/webhook
```

**Параметры:**
- `YUKASSA_SHOP_ID` - идентификатор магазина из ЛК ЮKassa
- `YUKASSA_SECRET_KEY` - секретный ключ из ЛК ЮKassa
- `YUKASSA_TEST_MODE` - `True` для тестов, `False` для боевого режима
- `YUKASSA_WEBHOOK_URL` - публичный URL для webhook (с ngrok или вашего сервера)

## Шаг 5: Запуск бота

1. Запустите бота:
   ```bash
   python main.py
   ```

2. Если используете ngrok, убедитесь что он запущен параллельно:
   ```bash
   ngrok http 8080
   ```

## Шаг 6: Тестирование

### 6.1. Создание тестового платежа

1. Откройте бота в Telegram
2. Нажмите **💳 Подписка**
3. Выберите тарифный план
4. Нажмите **Оплатить**

Вы будете перенаправлены на страницу оплаты ЮKassa.

### 6.2. Оплата тестовой картой

1. Введите тестовые данные карты: `5555 5555 5555 4477`
2. Заполните остальные поля произвольными данными
3. Подтвердите платеж

### 6.3. Проверка webhook

После успешной оплаты:
- ЮKassa отправит webhook на ваш URL
- Бот получит уведомление и активирует подписку
- Пользователь получит сообщение об успешной оплате

В логах вы должны увидеть:
```
Получено webhook уведомление от ЮKassa: {...}
Платеж xxx успешно оплачен
```

## Шаг 7: Переход в боевой режим

### 7.1. Получение боевых ключей

1. В личном кабинете ЮKassa переключитесь в **Боевой режим**
2. Пройдите модерацию (если требуется)
3. Получите новые ключи для боевого режима

### 7.2. Обновление конфигурации

Обновите `.env`:
```env
YUKASSA_SHOP_ID=your_production_shop_id
YUKASSA_SECRET_KEY=your_production_secret_key
YUKASSA_TEST_MODE=False
YUKASSA_WEBHOOK_URL=https://your-domain.com/yukassa/webhook
```

**ВАЖНО:**
- Используйте постоянный URL для webhook в продакшене
- Не используйте ngrok для боевого режима
- Убедитесь что SSL-сертификат валидный

## Работа с автоплатежами

### Сохранение карт

При первом платеже ЮKassa автоматически сохраняет платежный метод (карту) пользователя. Это настроено в коде:

```python
payment_data["save_payment_method"] = True
```

### Автоматическое продление

Для автоматического продления подписки бот может использовать сохраненную карту:

```python
payment_data["payment_method_id"] = saved_card_id
```

Пользователь НЕ будет перенаправлен на страницу оплаты - списание произойдет автоматически.

### Управление картами

Пользователи могут:
- Просмотреть привязанные карты: **💳 Подписка** → **💳 Мои карты**
- Отвязать карту: Выбрать карту → **🗑️ Отвязать карту**

## Структура базы данных

### Таблица payments

Хранит информацию о платежах:
- `payment_id` - ID платежа в ЮKassa
- `user_id` - ID пользователя Telegram
- `plan_id` - ID тарифного плана
- `amount` - Сумма платежа
- `status` - Статус платежа
- `paid` - Оплачен ли платеж

### Таблица payment_methods

Хранит привязанные карты:
- `payment_method_id` - ID платежного метода в ЮKassa
- `user_id` - ID пользователя
- `card_last4` - Последние 4 цифры карты
- `card_type` - Тип карты (Visa, MasterCard и т.д.)
- `is_active` - Активна ли карта

### Таблица subscriptions

Хранит подписки (существующая таблица, используется для ЮKassa):
- `user_id` - ID пользователя
- `plan_id` - ID тарифного плана
- `yandex_order_id` - ID платежа (используется как ссылка на payment_id)
- `status` - Статус подписки
- `start_date` / `end_date` - Даты начала и окончания

## Логирование

Все операции с платежами логируются. Проверяйте логи для отладки:

```bash
tail -f bot.log  # Linux/Mac
Get-Content bot.log -Wait  # Windows PowerShell
```

## Частые проблемы

### 1. Webhook не приходят

**Причины:**
- URL недоступен из интернета
- Неправильно настроен webhook в ЛК ЮKassa
- Порт 8080 заблокирован файрволом

**Решение:**
- Проверьте доступность URL через браузер
- Убедитесь что ngrok запущен (для тестов)
- Проверьте логи webhook сервера

### 2. Ошибка "ЮKassa не настроена"

**Причина:** Не указаны `YUKASSA_SHOP_ID` или `YUKASSA_SECRET_KEY` в `.env`

**Решение:** Проверьте `.env` файл и перезапустите бота

### 3. Платеж создается, но статус не обновляется

**Причина:** Webhook не доходит до бота

**Решение:**
- Проверьте логи webhook сервера
- Убедитесь что URL webhook правильный в ЛК ЮKassa

## Полезные ссылки

- [Документация ЮKassa](https://yookassa.ru/docs/support/payments)
- [API ЮKassa](https://yookassa.ru/docs/support/payments/onboarding/integration)
- [Тестовые данные](https://yookassa.ru/docs/support/payments/testing)
- [Webhook уведомления](https://yookassa.ru/docs/support/payments/payment-process#webhook)

## Техническая поддержка

При проблемах с интеграцией ЮKassa:
- Техподдержка ЮKassa: support@yookassa.ru
- Документация: https://yookassa.ru/docs
